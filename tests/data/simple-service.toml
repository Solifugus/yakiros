[component]
name = "simple-service"
type = "service"
binary = "/usr/bin/simple-daemon"
args = ["--config", "/etc/simple.conf"]

[requires]
capabilities = ["network"]

[provides]
capabilities = ["simple-api"]